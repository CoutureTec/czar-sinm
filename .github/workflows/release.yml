name: Release

# Dispara quando uma tag v* é publicada (ex: v0.2.0).
# Roda testes unitários e de integração antes de criar a release.
# Se qualquer teste falhar, a release NÃO é criada.

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  # ------------------------------------------------------------------
  # 1. Testes unitários (sem credenciais, rápido)
  # ------------------------------------------------------------------
  unit:
    name: Testes unitários
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependências
        run: pip install -e ".[dev]"

      - name: Executar testes unitários
        run: pytest --tb=short -q

  # ------------------------------------------------------------------
  # 2. Testes de integração (requer secrets)
  # ------------------------------------------------------------------
  integration:
    name: Testes de integração
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verificar se secrets estão disponíveis
        env:
          SECRET: ${{ secrets.SINM_USERNAME }}
        run: |
          if [ -z "$SECRET" ]; then
            echo "::error::Secrets não configurados. A release não pode ser criada sem validar a integração."
            exit 1
          fi

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependências
        run: pip install -e ".[dev]"

      - name: Criar .env para dados_auto_contidos
        run: |
          cat > exemplos/01_dados_auto_contidos/.env <<EOF
          SINM_AMBIENTE=hml
          SINM_USERNAME=${{ secrets.SINM_USERNAME }}
          SINM_PASSWORD=${{ secrets.SINM_PASSWORD }}
          SINM_CLIENT_ID=${{ secrets.SINM_CLIENT_ID }}
          SINM_CLIENT_SECRET=${{ secrets.SINM_CLIENT_SECRET }}
          EOF

      - name: Criar .env para dados_externos
        run: |
          cat > exemplos/02_dados_externos/.env <<EOF
          SINM_AMBIENTE=hml
          SINM_USERNAME=${{ secrets.SINM_USERNAME }}
          SINM_PASSWORD=${{ secrets.SINM_PASSWORD }}
          SINM_CLIENT_ID=${{ secrets.SINM_CLIENT_ID }}
          SINM_CLIENT_SECRET=${{ secrets.SINM_CLIENT_SECRET }}
          EOF

      - name: Exemplo dados_auto_contidos — fluxo completo
        working-directory: exemplos/01_dados_auto_contidos
        run: |
          OUTPUT=$(python exemplo.py --acao cadastraGleba)
          echo "$OUTPUT"
          CHAVE_NM=$(echo "$OUTPUT" | grep "^CHAVE_NM=" | cut -d= -f2)
          python exemplo.py --acao cadastraAnaliseSolo --chave_nm "$CHAVE_NM"
          python exemplo.py --acao cadastraSensoriamentoRemoto --chave_nm "$CHAVE_NM"
          python exemplo.py --acao consultaClassificacaoNM --chave_nm "$CHAVE_NM"

      - name: Exemplo dados_externos — fluxo completo
        working-directory: exemplos/02_dados_externos
        run: python exemplo.py --dados dados/processo_001

      - name: Limpar arquivos .env
        if: always()
        run: |
          rm -f exemplos/01_dados_auto_contidos/.env
          rm -f exemplos/02_dados_externos/.env

  # ------------------------------------------------------------------
  # 3. Cria a release — só executa se os dois jobs anteriores passarem
  # ------------------------------------------------------------------
  release:
    name: Criar release no GitHub
    runs-on: ubuntu-latest
    needs: [unit, integration]

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Criar release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true

  # ------------------------------------------------------------------
  # 4. Publica no PyPI — só executa após a release ser criada
  #
  # Usa Trusted Publisher (OIDC) — sem API token armazenado como secret.
  # Configuração única no PyPI:
  #   pypi.org → Account → Publishing → Add trusted publisher
  #   owner: CoutureTec | repo: czar-sinm | workflow: release.yml | env: pypi
  # ------------------------------------------------------------------
  pypi:
    name: Publicar no PyPI
    runs-on: ubuntu-latest
    needs: [release]
    environment: pypi

    permissions:
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar build
        run: pip install build

      - name: Construir pacote
        run: python -m build

      - name: Publicar no PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
